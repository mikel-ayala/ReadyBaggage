package com.grupo2.readybaggage

class Cliente (
    val email: String?,
    val pass: Int?,
    val nombre: String?
    ) {
    //fun copy(name: String = this.name, age: Int = this.age) = Empleado(name, age)
}